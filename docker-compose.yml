services:
  app:
    build: .
    container_name: python_app

    volumes:
      - ./workspace:/workspace

    tty: true
    stdin_open: true
    # 環境変数が必要な場合はここに追加
    environment:
      - PYTHONUNBUFFERED=1

    ports:
      - "28085:8000"
